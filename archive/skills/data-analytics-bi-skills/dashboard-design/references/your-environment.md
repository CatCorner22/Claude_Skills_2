# Your dashboard environment (sanitized template)

Wire in your current role here — the method is deliberately domain-neutral and attaches to
whatever dashboards you own wherever you work next: an analyst's revenue board, an attorney's
matter-status view, an ops manager's queue monitor, a developer's service scorecard. Fill
this in with your real setup; if any value is sensitive (real KPI targets, client names,
actual figures), keep it in `your-environment.private.md` instead — that suffix is
git-ignored. Commit only sanitized, structural examples.

- **BI tool(s):** <Power BI | Tableau | Oracle OTBI | Looker | Sheets/Excel>
- **Audiences & the decision each dashboard serves:**
  - <audience> → <decision / action driven>
- **Standard KPIs (name → formula → target → direction):**
  - <e.g. on-time-completion % → items closed within SLA / items closed → ≥ 95% → higher is better>
- **Where KPI definitions are published:** <definitions page, tooltip standard, data dictionary>
- **Refresh cadence & data latency:** <real-time | daily | monthly close>
- **Branding / layout conventions:** <colors that encode meaning, logo placement, grid>
- **Accessibility needs:** <color-blind-safe palette, font size, contrast>
- **Where it's published / who owns it:** <workspace, owner, review cadence>
- **Tool-specific build notes:** <how a view/tile/prompt is built in your BI tool>
