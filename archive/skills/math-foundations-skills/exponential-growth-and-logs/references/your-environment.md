# Your environment — exponential growth and logs

Fill this in so the skill computes with your real conventions instead of generic
ones. Keep committed content structural and sanitized: real figures, client names,
or internal projections belong in `your-environment.private.md` (git-ignored), never
in a committed file.

## Quantities you track that behave exponentially
- <quantity> — <typical period (month/quarter/year)> — <roughly what rate, if stable>
- <quantity> — <period> — <rate>

## Period conventions
- Fiscal year start: <month>
- "Month" means: <calendar month / 4-week cycle / business days>
- CAGR endpoint convention: <e.g., fiscal-year-end to fiscal-year-end>

## Display and rounding
- Rates shown to <n> decimal places; factors to <n>.
- House wording for CAGR vs simple growth: <convention>

## Chart-axis policy
- Default axis for growth series: <linear / log / both>
- When a log axis is required or forbidden: <house rule, if any>
