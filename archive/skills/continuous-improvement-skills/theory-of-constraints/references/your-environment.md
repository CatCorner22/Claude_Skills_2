# Your constraint environment (sanitized template)

Fill this in with your real system. If any value is sensitive (real people's names, vendors,
account details), keep it in `your-environment.private.md` instead — that suffix is git-ignored.
Commit only sanitized, structural examples.

- **System in scope:** <e.g. the month-end close; the AR collections pipeline; my weekly WIP>
- **Current constraint hypothesis:** <the task/resource everything waits on>
- **How it was verified at the gemba:** <what you observed, when, with whom — inference alone
  doesn't count>
- **Constraint type:** <physical / policy / market>
- **Exploit decisions in force:** <offloaded tasks, input quality checks, protected time,
  sequencing rules>
- **Subordination rules:** <release gate/rope, non-constraint measures, accepted idle time>
- **Buffer:** <what sits in front of the constraint, its size, how penetration is watched>
- **Elevation options (if still binding):** <cross-training, automation, capacity — with cost>
- **Throughput accounting definitions:** <what T, I, and OE mean for this system, and where the
  numbers come from>
- **Last re-identification date / known inertia rules:** <when step 1 was last re-run; rules
  sized to an old constraint that need retiring>
