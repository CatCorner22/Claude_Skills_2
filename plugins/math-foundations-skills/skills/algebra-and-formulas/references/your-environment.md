# Your environment — algebra and formulas

Fill this in with the formulas you actually solve and rearrange, so the skill works on
your real material instead of textbook stand-ins. Keep committed content structural and
sanitized — real rates, client terms, contract numbers, or anything sensitive goes in
`your-environment.private.md` (git-ignored), never here.

## Recurring formulas
For each formula you use, record it once with every symbol named in words and units:

| Formula | Symbols (name, meaning, units) | Which variable you usually solve for |
|---|---|---|
| e.g. `Q = F/(p − v)` | F = fixed cost ($/period); p = unit price ($/unit); v = unit variable cost ($/unit) | p, when pricing to a volume target |
|  |  |  |

## Spreadsheet conventions
- Which app and dialect (Excel / Google Sheets), and any precedence quirks you have hit.
- House parentheses rules, named-range conventions, where shared formulas live.

## Known trouble spots
- Formulas that have produced wrong answers before, and what the fix was.
- Word-problem patterns you translate often (pricing, staffing, allocations).
