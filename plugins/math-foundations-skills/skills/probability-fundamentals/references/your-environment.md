# Your environment — probability fundamentals

Fill this in so the skill answers with your real base rates and conventions instead
of generic ones. Keep committed content structural and sanitized: real incident
rates, client data, or account-level numbers belong in
`your-environment.private.md` (git-ignored), never in a committed file.

## Screens, flags, and alarms you interpret
- <screen/flag/alarm> — rough base rate of the true condition: <x%> — detection
  rate: <x%> — false-alarm rate: <x%> — where these figures come from: <source>
- <screen/flag/alarm> — <rates> — <source>

## Base-rate sources
- Where you look up base rates rather than assume them: <systems, logs, published
  figures>
- Base rates you assume without a source (flag for verification): <list>

## Table conventions
- House crowd size for natural-frequency tables: <e.g., 10,000>
- Rounding rule for read-off probabilities: <e.g., one decimal place>

## Ruin constraints
- Decisions where survival, not expected value, is the binding constraint (and the
  resource that must survive): <list>
