# Your feedback environment (sanitized template)

Fill in how you want the feedback loop to run. Keep anything sensitive in
`your-environment.private.md` (git-ignored).

- **How to acknowledge corrections:** <brief inline | explicit restatement + confirmation>
- **Quality bars that always apply:** <e.g. tie out totals; cite primary sources; show
  formulas; run the tests before claiming done>
- **Where lessons live:** <memory file / project instructions / artifact>
- **Reflection cadence:** <after major tasks | session end | on request only>
- **Standing avoidance rules:** <e.g. never email drafts without a subject line review>
- **When to ask sign-off:** <which method changes need explicit approval>
- **Preferred enforcement site:** <where a rule should live so it fires at the moment of action —
  memory entry | project instructions | a line in the artifact template | an automated check>
- **Inference threshold:** <how many silent sightings before an unstated preference is stored as
  standing; the house default is 2 for inferred, 1 for anything you said out loud>
- **Corrections you never want reflected on in-conversation:** <e.g. tone and formality notes —
  apply them and move on>
